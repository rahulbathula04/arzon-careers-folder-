/**
 * Legacy Nav component — now superseded by global ArzonHeader in __root.tsx.
 * Rendered as null to avoid double-navbar injection across existing routes.
 */
export function Nav() {
  return null;
}
